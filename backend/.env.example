# No server-side secrets are required. OpenRouter, Serper.dev, and Discord
# credentials are supplied by the client on each request (see the sidebar in
# the frontend) and are never persisted, per the "no auth / no database"
# requirement of the assignment.
#
# PORT is only used if your deployment platform requires it explicitly.
PORT=8000
